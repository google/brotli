environment:
  matrix:
    - BUILD_SYSTEM: other

build_script:
- if [true]==[true] (
    ECHO Building %BUILD_SYSTEM%
    SET "PATH=C:\mingw-w64\x86_64-6.3.0-posix-seh-rt_v5-rev1\mingw64\bin;%PATH%"
    mingw32-make --help
  )
